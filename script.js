const WA="51991429347";let pendingMessage="";function openWhatsApp(message){pendingMessage=message||"Hola, DAiMViXiOn. Quiero información.";const m=document.getElementById("termsModal");if(m){m.classList.add("open");document.body.classList.add("modal-open");const c=document.getElementById("termsAccept");if(c)c.checked=false}}function closeTerms(){document.getElementById("termsModal")?.classList.remove("open");document.body.classList.remove("modal-open")}function acceptTerms(){const c=document.getElementById("termsAccept");if(!c?.checked){alert("Debes aceptar los términos para continuar.");return}const url="https://web.whatsapp.com/send?phone="+WA+"&text="+encodeURIComponent(pendingMessage);window.open(url,"_blank","noopener,noreferrer");closeTerms()}document.querySelectorAll("[data-wa]").forEach(el=>el.addEventListener("click",e=>{e.preventDefault();openWhatsApp(el.dataset.wa)}));document.addEventListener("keydown",e=>{if(e.key==="Escape")closeTerms()});document.querySelector(".menu-toggle")?.addEventListener("click",()=>document.querySelector(".menu")?.classList.toggle("open"));document.querySelectorAll(".menu a").forEach(a=>a.addEventListener("click",()=>document.querySelector(".menu")?.classList.remove("open")));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("is-visible");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".section,.case-card,.eco-card,.research-node,.service-feature,.service-list article,.learning-steps article,.sports-services article,.result-card").forEach(x=>{x.classList.add("reveal");io.observe(x)});document.querySelectorAll(".intelligence-stage,.sports-field,.tech-orbit,.method-visual").forEach(el=>el.addEventListener("pointermove",e=>{const r=el.getBoundingClientRect();el.style.setProperty("--mx",((e.clientX-r.left)/r.width*100).toFixed(2)+"%");el.style.setProperty("--my",((e.clientY-r.top)/r.height*100).toFixed(2)+"%")}));
// Active navigation: keeps the multi-page architecture explicit and accessible.
const currentFile = (location.pathname.split('/').pop() || 'index.html').toLowerCase();
document.querySelectorAll('.menu a').forEach(a=>{
  const target=(a.getAttribute('href')||'').split('#')[0].toLowerCase();
  if(target===currentFile) a.setAttribute('aria-current','page');
});
